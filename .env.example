# Anthropic API 키 — /ai/analyze, /ai/suggest-fixtures, MCP ai_analyze 도구에 필요
# https://console.anthropic.com 에서 발급
ANTHROPIC_API_KEY=sk-ant-여기에-실제-키-입력

# (선택) 한글 깨짐 방지 — Claude Desktop이 stdio로 MCP 서버를 기동할 때
PYTHONIOENCODING=utf-8
