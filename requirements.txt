# ── FastAPI 검증 서버 ──────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9      # UploadFile (/import/excel)

# ── MCP 서버 (Claude Desktop 연동) ─────────────
mcp[cli]>=1.2.0

# ── Claude API (AI 자연어 피드백) ──────────────
anthropic>=0.40.0

# ── 규칙 DB 변환 ───────────────────────────────
openpyxl>=3.1.0              # excel_to_json.py
python-dotenv>=1.0.0         # .env 자동 로드 (선택, 없어도 동작)

# ── 테스트 ─────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0                # fastapi.testclient 의존성
